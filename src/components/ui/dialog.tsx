import * as React from "react";
import { Dialog as BaseDialog, DialogTrigger, DialogContent as BaseContent, DialogHeader as BaseHeader, DialogTitle as BaseTitle } from "@radix-ui/react-dialog";

export const Dialog = BaseDialog;
export const DialogContent = BaseContent;
export const DialogHeader = BaseHeader;
export const DialogTitle = BaseTitle;
export const DialogTriggerButton = DialogTrigger;
